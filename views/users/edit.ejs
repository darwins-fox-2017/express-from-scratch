<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <title>Create</title>
</head>
<form class="ui form wide" action="/users/<%= user.id %>/update" method="post">
    <div class="field">
        <label>First Name</label>
        <input name="firstname" placeholder="First Name" type="text" value="<%= user.firstname %>">
    </div>
    <div class="field">
        <label>Last Name</label>
        <input name="lastname" placeholder="Last Name" type="text" value="<%= user.lastname %>">
    </div>
    <div class="field">
        <label>Phone</label>
        <input name="phone" placeholder="Phone" type="text" value="<%= user.phone %>">
    </div>
    <button class="ui button" type="submit">Update User</button>
</form>

</html>
